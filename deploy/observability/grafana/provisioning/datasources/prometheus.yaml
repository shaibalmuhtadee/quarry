apiVersion: 1

prune: true

datasources:
  - name: Prometheus
    uid: quarry-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      httpMethod: POST
      timeInterval: 2s
