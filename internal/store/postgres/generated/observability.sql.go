// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: observability.sql

package postgresdb

import (
	"context"
)

const getQueueSnapshot = `-- name: GetQueueSnapshot :one
WITH clock AS (
    SELECT statement_timestamp() AS now
)
SELECT
    COUNT(*) FILTER (WHERE status = 'queued')::bigint AS queued_jobs,
    COUNT(*) FILTER (WHERE status = 'retry_wait')::bigint AS retry_wait_jobs,
    COALESCE(
        GREATEST(
            EXTRACT(EPOCH FROM (
                (SELECT now FROM clock)
                - MIN(available_at) FILTER (
                    WHERE status IN ('queued', 'retry_wait')
                      AND available_at <= (SELECT now FROM clock)
                )
            )),
            0
        ),
        0
    )::double precision AS oldest_eligible_age_seconds,
    COUNT(*) FILTER (WHERE status = 'running')::bigint AS active_jobs,
    (SELECT COUNT(*) FROM workers WHERE state = 'active')::bigint AS active_workers
FROM jobs
`

type GetQueueSnapshotRow struct {
	QueuedJobs               int64
	RetryWaitJobs            int64
	OldestEligibleAgeSeconds float64
	ActiveJobs               int64
	ActiveWorkers            int64
}

func (q *Queries) GetQueueSnapshot(ctx context.Context) (GetQueueSnapshotRow, error) {
	row := q.db.QueryRow(ctx, getQueueSnapshot)
	var i GetQueueSnapshotRow
	err := row.Scan(
		&i.QueuedJobs,
		&i.RetryWaitJobs,
		&i.OldestEligibleAgeSeconds,
		&i.ActiveJobs,
		&i.ActiveWorkers,
	)
	return i, err
}
